# ─── Scheduling ─────────────────────────────────────────────────────────────
# Cron expression (default: every 6 hours)
# Examples:
#   0 */6 * * *  = Every 6 hours
#   0 9 * * *    = Daily at 9 AM
#   0 9 * * 1-5  = Weekdays at 9 AM
CRON_SCHEDULE=0 */6 * * *

# Run immediately on startup (true/false)
RUN_ON_STARTUP=true

# Maximum leads to collect per run (per source)
MAX_LEADS_PER_RUN=100

# ─── Email Notifications ────────────────────────────────────────────────────
# Enable email summaries (true/false)
ENABLE_EMAIL=true

# Gmail credentials (use App Password, not account password)
# https://myaccount.google.com/apppasswords
GMAIL_USER=your-email@gmail.com
GMAIL_APP_PASSWORD=your-16-char-app-password

# Email recipient (defaults to GMAIL_USER if not set)
NOTIFICATION_EMAIL=recipient@example.com

# ─── Enrichment APIs ────────────────────────────────────────────────────────
# Hunter.io API key (for email discovery)
# https://hunter.io/api_keys
HUNTER_API_KEY=your_hunter_api_key_here

# Clearbit API key (for company enrichment)
# https://dashboard.clearbit.com/api
CLEARBIT_API_KEY=your_clearbit_api_key_here

# ─── LinkedIn Scraper (Optional) ────────────────────────────────────────────
# LinkedIn credentials for company page scraping
# IMPORTANT: LinkedIn may block automated access - use at your own risk
# Consider using LinkedIn Sales Navigator API instead for production
LINKEDIN_EMAIL=your-linkedin-email@example.com
LINKEDIN_PASSWORD=your-linkedin-password
