# ================================================================================================
# MARKETING TEAM ENVIRONMENT VARIABLES
# ================================================================================================
# Copy this to ~/.config/openclaw/.env or merge with existing .env.local
# Remove the .template extension and fill in your actual values
#
# Location in WSL: ~/.config/openclaw/.env
# Location in Windows: C:\Users\SPilcher\OpenClaw2.0 for linux - Copy\.env.local
# ================================================================================================

# ================================================================================================
# WORDPRESS CONFIGURATION (Required for CMS Publisher)
# ================================================================================================
WORDPRESS_URL=https://www.hoaprojectfunding.com
WORDPRESS_USER=your_admin_username
WORDPRESS_APP_PASSWORD=xxxx-xxxx-xxxx-xxxx

# How to get WordPress App Password:
# 1. Log in to WordPress admin
# 2. Go to Users → Profile
# 3. Scroll to "Application Passwords"
# 4. Enter name "OpenClaw" and click "Add New Application Password"
# 5. Copy the generated password (format: xxxx xxxx xxxx xxxx)

# ================================================================================================
# SOCIAL MEDIA APIs (Required for Social Engagement Monitor)
# ================================================================================================

# LinkedIn
LINKEDIN_ACCESS_TOKEN=your_linkedin_token
LINKEDIN_ORGANIZATION_ID=your_org_id
# Get token: https://www.linkedin.com/developers/apps
# Scopes needed: r_organization_social, w_organization_social

# Twitter/X
TWITTER_BEARER_TOKEN=your_bearer_token
TWITTER_USER_ID=your_user_id
# Get token: https://developer.twitter.com/en/portal/dashboard
# V2 API with read/write permissions

# Facebook
FACEBOOK_PAGE_ACCESS_TOKEN=your_page_token
FACEBOOK_PAGE_ID=your_page_id
# Get token: https://developers.facebook.com/tools/explorer
# Permissions: pages_read_engagement, pages_manage_posts

# ================================================================================================
# EMAIL SERVICE PROVIDER (Required for Email Campaigns)
# ================================================================================================
EMAIL_ESP=mailchimp
# Options: mailchimp, sendgrid, convertkit, activecampaign

# Mailchimp
MAILCHIMP_API_KEY=your_api_key_here
MAILCHIMP_SERVER_PREFIX=us1
# Server prefix is in your API key (e.g., us1, us2, etc.)
MAILCHIMP_LIST_ID=your_list_id

# SendGrid (alternative)
# SENDGRID_API_KEY=SG.xxxxx
# SENDGRID_FROM_EMAIL=marketing@hoaprojectfunding.com
# SENDGRID_FROM_NAME=HOA Project Funding

# ConvertKit (alternative)
# CONVERTKIT_API_KEY=your_api_key
# CONVERTKIT_API_SECRET=your_api_secret
# CONVERTKIT_FORM_ID=your_form_id

# ================================================================================================
# TELEGRAM NOTIFICATIONS (Optional but Recommended)
# ================================================================================================
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHAT_ID=123456789

# How to set up Telegram bot:
# 1. Message @BotFather on Telegram
# 2. Send /newbot and follow prompts
# 3. Copy the bot token
# 4. Start a chat with your bot
# 5. Get your chat ID from: https://api.telegram.org/bot<TOKEN>/getUpdates

# ================================================================================================
# MIXPOST INTEGRATION (Optional - for advanced social scheduling)
# ================================================================================================
MIXPOST_URL=http://localhost:8080
MIXPOST_API_TOKEN=your_mixpost_token
# Only needed if you want to use Mixpost for social media queue management

# ================================================================================================
# SMTP CONFIGURATION (Already configured for digest emails)
# ================================================================================================
# You likely already have these from earlier setup:
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM=your-email@gmail.com

# ================================================================================================
# MARKETING AGENT CONFIGURATION
# ================================================================================================

# Content Writer
CONTENT_WORD_COUNT_MIN=1200
CONTENT_WORD_COUNT_MAX=1500
CONTENT_TONE=professional-approachable
TARGET_KEYWORDS=HOA financing,reserve fund loans,special assessment alternatives

# Social Media
SOCIAL_LINKEDIN_MAX_LENGTH=200
SOCIAL_TWITTER_MAX_LENGTH=280
SOCIAL_FACEBOOK_MAX_LENGTH=150

# CMS Publisher
CMS_DEFAULT_STATUS=draft
# Options: draft, publish, future (for scheduled)
CMS_AUTO_PUBLISH=false
CMS_CATEGORY_DEFAULT=Blog

# Email Campaigns
EMAIL_NURTURE_SEQUENCE_ENABLED=true
EMAIL_RE_ENGAGEMENT_DAYS=30
EMAIL_NEWSLETTER_DAY=tuesday

# ================================================================================================
# OPENAI API (if using separate API key for agents)
# ================================================================================================
# OPENAI_API_KEY=sk-xxxxx
# Only needed if not using Anthropic API for all agents

# ================================================================================================
# VERIFICATION COMMANDS
# ================================================================================================
# After filling in values, verify configuration:
#
# Test WordPress connection:
#   curl -u "${WORDPRESS_USER}:${WORDPRESS_APP_PASSWORD}" \
#     "${WORDPRESS_URL}/wp-json/wp/v2/posts?per_page=1"
#
# Test Telegram bot:
#   curl "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage?chat_id=${TELEGRAM_CHAT_ID}&text=Test"
#
# Test Mailchimp:
#   curl -X GET "https://${MAILCHIMP_SERVER_PREFIX}.api.mailchimp.com/3.0/lists/${MAILCHIMP_LIST_ID}" \
#     -u "anystring:${MAILCHIMP_API_KEY}"
#
# ================================================================================================
